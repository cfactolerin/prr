---
description: Start a PRR review for a GitHub pull request.
agent: prr-orchestrator
---

Use the skill tool to load `prr-start`, then follow that skill exactly in the current primary thread. Pass the user's command arguments to the skill exactly as `$ARGUMENTS`.
